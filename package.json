{
  "repository": "https://github.com/claygorman/openpro.git",
  "version": "0.0.1",
  "license": "MIT",
  "private": true,
  "scripts": {
    "pnpm:install": "pnpm run pnpm:install:backend && pnpm run pnpm:install:frontend",
    "pnpm:install:backend": "cd backend && pnpm install",
    "pnpm:install:frontend": "cd frontend && pnpm install",
    "dev": "docker-compose -f docker-compose-local.yml up --build --force-recreate --remove-orphans"
  },
  "packageManager": "pnpm@8.10.2"
}
